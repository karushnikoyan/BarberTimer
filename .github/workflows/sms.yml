name: sms

on: push

jobs:
  build:
  
    runs-on: ubuntu-latest
    
    steps: 
    - uses: actions/checkout@v3
    
    - name: 'Sending SMS Notification'
      uses: twilio-labs/actions-sms@v1
      with:
        fromPhoneNumber: '+1(234)5678901'
        toPhoneNumber: '+37433777992'
        message: 'Hello from Twilio MR. Karush'
      env:
        TWILIO_ACCOUNT_SID: ${{ SK8fccd1f097191bb71f7c08ce94bc3134 }}
        TWILIO_API_KEY: ${{'[Standard]'}}
        TWILIO_API_SECRET: ${{pumdJZPkXAZkI2V6AJLuYJnCF0aaAdU1}}
    
